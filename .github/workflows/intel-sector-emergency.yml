name: Alaska Intel - Emergency Sector

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches: [ master ]

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  pipeline:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/alaskaintel/alaskaintel-json/alaskaintel-pipeline:latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout alaskaintel-data
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch Emergency APIs
        run: |
          python3 pipelines/fetch_earthquakes.py
          python3 pipelines/fetch_weather.py
          python3 pipelines/fetch_wildfires.py
          python3 pipelines/scrape_ast.py
          python3 pipelines/fetch_stolen_vehicles_api.py

      - name: Fetch Emergency RSS
        env:
          INGEST_SECRET: ${{ secrets.INGEST_SECRET }}
        run: |
          python3 pipelines/monitor_feeds.py
          python3 pipelines/fetch_intel.py --category emergency,safety,weather

      - name: Commit + Push Data
        id: git_sync
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global user.name "AlaskaIntel-Bot"
          git config --global user.email "bot@alaskaintel.com"
          git add data/ public/ || true
          
          if ! git diff --staged --quiet; then
            git commit -m "intel: emergency sector sync $(date -u +'%Y-%m-%d %H:%M UTC')"
            for i in 1 2 3 4 5; do
              git pull --rebase origin master
              if git push origin master; then break; fi
              sleep 5
            done
            echo "HAS_CHANGES=true" >> $GITHUB_OUTPUT
          else
            echo "HAS_CHANGES=false" >> $GITHUB_OUTPUT
          fi

